SELECT (COUNT(*) AS ?benchmark_count)
FROM <https://crc1625.mdi.ruhr-uni-bochum.de/graph>
WHERE {
    SELECT ?ML
    WHERE {
      ?ML a/rdfs:subClassOf* :MaterialsLibrary.

      MINUS {
        ?ML pmdco:relatesToElement ?element.
        ?element chebi:formula ?f.
        FILTER(?f IN ("Ag", "Au"))
      }
    }
}