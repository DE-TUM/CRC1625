SELECT (COUNT(?sample) AS ?n_samples) FROM <https://crc1625.mdi.ruhr-uni-bochum.de/graph> WHERE {
    ?sample a :MaterialsLibrary

    FILTER NOT EXISTS {
        ?sample pmdco:composedOf ?piece.
    }

    FILTER NOT EXISTS {
        ?another_sample pmdco:composedOf ?sample.
    }
}