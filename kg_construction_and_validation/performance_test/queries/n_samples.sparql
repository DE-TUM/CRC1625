SELECT (COUNT(?sample) AS ?n_samples) FROM <https://crc1625.mdi.ruhr-uni-bochum.de/graph> WHERE {
    ?sample a crc:MaterialsLibrary

    FILTER NOT EXISTS {
        ?sample crc:composedOf ?piece.
    }

    FILTER NOT EXISTS {
        ?another_sample crc:composedOf ?sample.
    }
}