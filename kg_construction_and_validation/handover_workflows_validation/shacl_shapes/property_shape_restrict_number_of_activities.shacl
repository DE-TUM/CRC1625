  sh:property [
    sh:path (crc:substep [ sh:zeroOrMorePath crc:nextStep ] crc:substep) ;

    sh:qualifiedValueShape [
      sh:class crc:CharacterizationActivityInstance ;
    ] ;

    sh:qualifiedMinCount {number_of_activities} ;
    sh:qualifiedMaxCount {number_of_activities} ;
    sh:message "There is a different number of activities done in the handover group. We expected {number_of_activities}"
  ] ;