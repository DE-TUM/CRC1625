prefixes: {prefixes}

sources:
  sql_source: {sources}

mappings:
  handover_chain:
    sources: sql_source
    s: handover:$(HandoverId)
    po:
      - [crc:nextStep, handover:$(NextHandoverId)~iri]