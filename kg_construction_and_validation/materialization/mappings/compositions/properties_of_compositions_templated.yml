prefixes: {prefixes}

sources:
  sql_source: {sources}

mappings:
  composition_properties:
    sources: sql_source
    s: EDX_composition:$(CompositionId)_in_MA_$(MeasurementArea)
    po:
      - [:$(propertyname), $(value), xsd:float] # TODO improve