# Query that retrieves all possible characterization activities (e.g. XRD, XPS...)
#
# You can use these types to filter them, as can be found in `handovers_with_specific_characterization_activity.sparql`
#
SELECT ?characterization_activity
FROM <https://crc1625.mdi.ruhr-uni-bochum.de/graph>
WHERE {
  ?characterization_activity rdfs:subClassOf ?subclass.
  FILTER (?subclass IN (pmdco:AnalysingProcess, pmdco:MicroscopyProcess))
}